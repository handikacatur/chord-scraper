## How to run?

1. Install all dependencies
```
yarn install
```

2. Copy config file
```
cp example.config.yaml .config.yaml
```

3. Change `target-url` and `artist-name` 

4. Run command
```
yarn start
```